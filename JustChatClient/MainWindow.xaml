<Window x:Name="JCC" x:Class="JustChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JustChatClient"
        mc:Ignorable="d"
        Title="JCC - Just Chat Client" 
        WindowStyle="SingleBorderWindow" 
        ResizeMode="CanResize" 
        SizeToContent="WidthAndHeight" 
        Closing="Window_Closing" 
        Initialized="Window_Initialized" 
        HorizontalAlignment="Stretch" 
        Icon="JustChat.ico" 
        Height="500" Width="495" ScrollViewer.VerticalScrollBarVisibility="Disabled" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" MinWidth="495" MinHeight="500">
    <Grid 
        ClipToBounds="True">
        <TextBox 
            x:Name="inputBox" 
            HorizontalAlignment="Right"  
            TextWrapping="Wrap" 
            VerticalAlignment="Bottom" 
            Width="285" 
            Height="24" 
            KeyDown="inputBox_KeyDown" 
            Margin="0,0,10,25" BorderThickness="2,2,2,2"
        />
        <TextBox 
            x:Name="userName" 
            HorizontalAlignment="Left"  
            Text="user" 
            TextWrapping="NoWrap" 
            VerticalAlignment="Top" 
            Margin="57,10,0,0" BorderBrush="#00ABADB3" MaxLines="1" Height="26" VerticalContentAlignment="Center" HorizontalContentAlignment="Stretch" VerticalScrollBarVisibility="Disabled"
        />
        <ListView 
            x:Name="chatLog"  
            ItemsSource="{Binding Messages}" 
            Focusable="False" 
            Margin="0,44,10,54" 
            VerticalContentAlignment="Center" 
            BorderThickness="2,2,2,2" SelectionMode="Extended" HorizontalAlignment="Right" Width="285" Background="{x:Null}" >
            <ListView.ItemContainerStyle>
                <Style 
                    TargetType="{x:Type ListBoxItem}"
                    BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter 
                        Property="DockPanel.Dock"
                        Value="Bottom"
                    />
                </Style>
            </ListView.ItemContainerStyle>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel 
                        LastChildFill="False" 
                        ClipToBounds="False" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel x:Name="chatMessage" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Stretch" 
                        IsEnabled="True" 
                        ClipToBounds="False" 
                        Focusable="False" 
                        Width="295">
                        <Label 
                            x:Name="messageSender" 
                            Content="{Binding User}" 
                            FontWeight="Bold" 
                            FontStyle="Italic"/>
                        <TextBlock 
                            x:Name="messageText" 
                            Text="{Binding Message}" 
                            TextWrapping="Wrap" 
                            TextTrimming="WordEllipsis" 
                            VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Label Content="Name:" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontWeight="Bold"/>
        <StatusBar Height="20" VerticalAlignment="Bottom">
            <CheckBox 
            x:Name="isConnected" 
            Content="Connection" 
            HorizontalAlignment="Left" 
            VerticalAlignment="Top" 
            IsChecked="{Binding IsConnected}" 
            Focusable="False" IsEnabled="False" BorderBrush="#00707070"
            >
                <CheckBox.Foreground>
                    <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.GrayTextColorKey}}"/>
                </CheckBox.Foreground>
            </CheckBox>
        </StatusBar>
        <ListView HorizontalAlignment="Left" Margin="10,44,0,25" Width="185" BorderThickness="2,2,2,2" Background="Transparent" BorderBrush="#FFABADB3">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
